## to run
create .venv:
`python -m venv .venv`

activated it using:
`source .venv/bin/activate`

install packages:
`pip install -r requirements.txt`

start app:
`python stateserver.py`

deactivate it using the following when you are done:
`deactivate`